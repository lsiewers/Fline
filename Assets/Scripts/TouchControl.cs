using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class TouchControl : MonoBehaviour
{
    public GameObject text;
    public GameObject lineText;

    // Update is called once per frame
    void Update()
    {
        //if (Input.touchCount > 0) {
        //    text.GetComponent<TextMeshProUGUI>().SetText(Input.GetTouch(0).position.ToString());

        //    var HitLine = FollowLine.Instance.points.Contains(Input.GetTouch(0).position);
        //    lineText.GetComponent<TextMeshProUGUI>().SetText(FollowLine.Instance.points[FollowLine.Instance.points.Count/2].ToString());
        //}
    }

}
